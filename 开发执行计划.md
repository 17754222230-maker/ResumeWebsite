# 个人简历 & 智能知识站 — 开发执行计划

---

## 一、项目概述

构建一个展示个人技术能力、项目经历，并内置 AI 知识助手的个人网站。站点体现简约、现代、略带东方韵味的视觉风格，色彩遵从八字命理喜用色（金水）。

| 项目 | 内容 |
|------|------|
| 项目名称 | 个人简历 & 智能知识站 (Personal Portfolio & AI Knowledge Agent) |
| 技术栈 | Next.js 14 (App Router) + TypeScript + Tailwind CSS + shadcn/ui |
| AI 方案 | Vercel AI SDK / FastAPI + 国产大模型 |
| 部署目标 | 阿里云 OSS + CDN（前端） / 阿里云 ECS 或函数计算（Agent） |
| 面向人群 | 技术面试官、潜在雇主、技术同行 |

---

## 二、色彩与设计规范（八字命理映射）

### 2.1 配色体系

| 角色 | 色值 | 五行 | 寓意 | 用途 |
|------|------|------|------|------|
| **主色** | `#0B1D3A` / `#102A43` | 水 | 财富、流动 | 导航栏、页脚、主背景区 |
| **辅色** | `#D4AF37` / `#C5A860` | 金 | 才华泄秀 | 按钮、强调文字、图标高亮 |
| **背景** | `#F0F4F8` / `#E8EDF2` | 水（浅） | 清爽呼吸感 | 页面底色、卡片背景 |
| **文字** | `#1A202C` / `#FFFFFF` | — | — | 正文 / 深色背景上文 |
| **点缀** | `#A0D2DB` / `#7EB8C9` | 水木 | 点睛 | Logo、水波纹装饰线 |

### 2.2 禁用色

大面积红 (`#FF0000` 系)、橙 (`#FFA500` 系)、黄 (`#FFFF00` 系)、土棕 (`#8B4513` 系) — 对应火土，日主忌之。

### 2.3 设计风格

- 现代科技感 + 东方雅致留白
- 质感方向：微水波纹、金属光泽、磨砂玻璃 (backdrop-filter: blur)
- 响应式设计，移动端优先

---

## 三、功能需求清单

### 3.1 主页面（个人简介）— MVP 阶段实现

- [ ] **英雄区**：个人姓名、Title、一句话 Slogan + 个人形象/头像
- [ ] **技术栈标签云**：以图标/标签展示核心技术能力
- [ ] **个人优势区**：简短条目突出技术亮点与软技能
- [ ] **项目卡片矩阵**：缩略图 + 名称 + 简介 + 技术标签，可点击进入详情

### 3.2 项目详情页 — MVP 阶段实现

- [ ] 静态生成 (SSG)，每个项目独立页面
- [ ] 包含：项目背景、个人职责、技术栈、关键成果、链接

### 3.3 AI 智能助理 — MVP 阶段实现

- [ ] 悬浮于主页面右下或右侧边栏
- [ ] 对话式界面，回答访客关于"我"的问题
- [ ] 工具调用：`get_skill_list()` / `search_projects()` / `get_personal_info()`
- [ ] 知识来源：结构化 Markdown/YAML 知识卡片

### 3.4 内容管理 — MVP 阶段实现

- [ ] 项目与知识卡片以结构化文件存储（Markdown + YAML Frontmatter / JSON）
- [ ] 知识卡片分类：`技能` / `项目` / `经历` / `其他`

---

## 四、技术架构方案

### 4.1 架构总览

```
┌─────────────────────────────────────────────────┐
│                 浏览器 (Client)                   │
│  ┌───────────────────────────────────────────┐   │
│  │  Next.js 14 App Router (SSG + CSR)        │   │
│  │  Tailwind CSS + shadcn/ui + Framer Motion │   │
│  └────────────┬──────────────────────────┬───┘   │
│               │ 用户交互                  │ AI 对话│
│               ▼                          ▼       │
│         ┌─────────┐           ┌──────────────┐   │
│         │ 项目页面 │           │ AI Assistant │   │
│         │ (SSG)   │           │ (CSR + API)  │   │
│         └─────────┘           └──────┬───────┘   │
└──────────────────────────────────────┼───────────┘
                                       │ API 请求
                                       ▼
┌──────────────────────────────────────────────────┐
│            后端服务 (API Route / ECS)              │
│  ┌──────────────────────────────────────┐        │
│  │  AI Agent 服务                        │        │
│  │  ┌─────────┐ ┌──────────┐ ┌───────┐ │        │
│  │  │ 工具调用 │ │ 知识检索  │ │ LLM   │ │        │
│  │  └─────────┘ └──────────┘ └───────┘ │        │
│  └──────────────────────────────────────┘        │
└──────────────────────────────────────────────────┘
```

### 4.2 技术选型对照

| 模块 | 方案 A（Node 全栈，推荐 MVP） | 方案 B（Python 微服务，后期迭代） |
|------|-------------------------------|-----------------------------------|
| 前端框架 | Next.js 14 App Router + TypeScript | 同左 |
| UI/样式 | Tailwind CSS + shadcn/ui | 同左 |
| 动画 | Framer Motion | 同左 |
| 内容格式 | Markdown + gray-matter (Frontmatter) | 同左 |
| AI Agent | Next.js API Route + Vercel AI SDK | FastAPI + LangChain |
| 大模型 | DeepSeek / 智谱 GLM-4（国产） | OpenAI / 国产模型 |
| 知识检索 | 初期跳过向量库，Function Calling + 结构化 JSON 直接喂给 LLM | Qdrant / pgvector |
| 部署前端 | 阿里云 OSS + CDN（静态导出） | 同左 |
| 部署后端 | 阿里云函数计算 FC | 阿里云 ECS + Docker |

### 4.3 AI 知识问答流程

```
用户提问 → Agent 接收 → 判断是否需要调用工具
  ├─ 固定类问题（"你会什么？"）→ get_skill_list() 返回结构化数据
  ├─ 项目类问题（"做过什么项目？"）→ search_projects() 检索项目卡片
  └─ 综合类问题 → 读取相关知识卡片 JSON → LLM 生成回答
```

**初期 MVP 原则**：不引入向量数据库，知识卡片构建时解析为 JSON 缓存，Agent 通过工具函数直接读取。

---

## 五、项目目录结构

```
resume-website/
├── public/                    # 静态资源
│   ├── images/                # 图片资源
│   │   ├── avatar.webp        # 个人头像
│   │   ├── hero-bg.webp       # 英雄区背景
│   │   └── projects/          # 项目截图
│   └── favicon.ico
│
├── src/
│   ├── app/                   # Next.js App Router
│   │   ├── layout.tsx         # 全局布局（导航栏 + 页脚）
│   │   ├── page.tsx           # 主页（英雄区 + 技术栈 + 项目卡片）
│   │   ├── globals.css        # 全局样式 + Tailwind 层
│   │   ├── projects/          # 项目详情页（SSG）
│   │   │   └── [slug]/
│   │   │       └── page.tsx
│   │   ├── api/               # API Routes
│   │   │   └── chat/          # AI 对话接口
│   │   │       └── route.ts
│   │   └── loading.tsx        # 全局加载态
│   │
│   ├── components/            # 组件
│   │   ├── ui/                # shadcn/ui 基础组件
│   │   ├── HeroSection.tsx    # 英雄区组件
│   │   ├── TechStack.tsx      # 技术栈展示
│   │   ├── ProjectCard.tsx    # 项目卡片
│   │   ├── ProjectGrid.tsx    # 项目卡片网格
│   │   ├── AiAssistant.tsx    # AI 助手悬浮组件
│   │   ├── ChatDialog.tsx     # 对话弹窗
│   │   ├── Navbar.tsx         # 导航栏
│   │   └── Footer.tsx         # 页脚
│   │
│   ├── lib/                   # 工具函数
│   │   ├── utils.ts           # 通用工具
│   │   └── knowledge.ts       # 知识卡片读取/检索工具
│   │
│   └── data/                  # 结构化数据（知识卡片）
│       ├── profile.yaml       # 个人信息
│       ├── skills.yaml        # 技能列表
│       ├── projects/          # 项目卡片（Markdown）
│       │   ├── project-1.md
│       │   └── project-2.md
│       └── experiences/       # 工作经历
│           └── experience-1.md
│
├── tailwind.config.ts         # Tailwind 配置（含自定义色彩）
├── next.config.ts
├── tsconfig.json
├── package.json
└── README.md
```

---

## 六、开发阶段与任务分解

### 第一阶段：项目初始化与环境搭建（预计 1 天）

| # | 任务 | 产出 |
|---|------|------|
| 1.1 | 使用 `create-next-app` 初始化项目，选择 TypeScript + Tailwind | 项目骨架 |
| 1.2 | 配置 `tailwind.config.ts`：锁定八字喜用色，禁用默认主题色 | 色彩系统就绪 |
| 1.3 | 安装 shadcn/ui 基础组件 (Button, Card, Dialog, Input) | UI 组件库 |
| 1.4 | 安装 Framer Motion | 动画能力 |
| 1.5 | 配置 `next.config.ts`：静态导出相关设置 | 构建配置 |
| 1.6 | 搭建基础目录结构 (`src/app`, `src/components`, `src/data`, `src/lib`) | 目录就绪 |

### 第二阶段：数据层 — 知识卡片系统（预计 1 天）

| # | 任务 | 产出 |
|---|------|------|
| 2.1 | 创建 `src/data/profile.yaml`：姓名、Title、Slogan、简介 | 个人信息数据 |
| 2.2 | 创建 `src/data/skills.yaml`：技能列表（分类 + 熟练度） | 技能数据 |
| 2.3 | 创建 2-3 个示例项目 Markdown 文件（含 Frontmatter） | 项目数据 |
| 2.4 | 创建 2-3 个工作经历 Markdown 文件 | 经历数据 |
| 2.5 | 编写 `src/lib/knowledge.ts`：读取并解析所有知识卡片 | 数据读取工具 |

### 第三阶段：UI 组件开发 — 主页（预计 2 天）

| # | 任务 | 产出 |
|---|------|------|
| 3.1 | 实现 `Navbar.tsx`：响应式导航栏（移动端折叠菜单） | 导航栏 |
| 3.2 | 实现 `HeroSection.tsx`：姓名 + Title + Slogan + 头像 + 背景动效 | 英雄区 |
| 3.3 | 实现 `TechStack.tsx`：技能标签云/图标网格 + 动画入场 | 技术栈区 |
| 3.4 | 实现 `ProjectCard.tsx` + `ProjectGrid.tsx`：卡片网格 + hover 动效 | 项目展示区 |
| 3.5 | 实现 `Footer.tsx`：联系信息 + 社交链接 | 页脚 |
| 3.6 | 组装 `src/app/page.tsx`：组合各组件，完成主页 | 主页完成 |

### 第四阶段：UI 组件开发 — 项目详情页（预计 1 天）

| # | 任务 | 产出 |
|---|------|------|
| 4.1 | 实现 `src/app/projects/[slug]/page.tsx`：静态生成（generateStaticParams） | 项目详情页 |
| 4.2 | 详情页布局：项目背景、职责、技术栈、成果、链接 | 详情页完成 |
| 4.3 | 添加上下项目导航 / 返回列表 | 导航完善 |

### 第五阶段：AI 智能助理（预计 2 天）

| # | 任务 | 产出 |
|---|------|------|
| 5.1 | 实现 `AiAssistant.tsx`：右下悬浮按钮 + 展开/收起动画 | 悬浮组件 |
| 5.2 | 实现 `ChatDialog.tsx`：对话气泡、输入框、加载态 | 对话界面 |
| 5.3 | 注册大模型 API Key (DeepSeek / 智谱)，测试连通性 | API 就绪 |
| 5.4 | 实现 `src/app/api/chat/route.ts`：AI Agent 接口 | API Route |
| 5.5 | 实现工具函数：`get_skill_list()` / `search_projects()` / `get_personal_info()` | 工具集 |
| 5.6 | 联调：前端对话 → API → LLM → 工具调用 → 回答返回 | 联调完成 |

### 第六阶段：部署上线（预计 1 天）

| # | 任务 | 产出 |
|---|------|------|
| 6.1 | 配置 Next.js 静态导出 (`output: 'export'`) | 静态构建 |
| 6.2 | 阿里云 OSS 创建 Bucket + 上传静态文件 | 前端部署 |
| 6.3 | 阿里云 CDN 加速绑定域名 | CDN 配置 |
| 6.4 | 部署 AI Agent 服务到阿里云函数计算 FC | Agent 部署 |
| 6.5 | HTTPS 证书配置 + 全链路测试 | 安全就绪 |

---

## 七、开发工具与账号清单

### 7.1 必备软件

| 工具 | 用途 |
|------|------|
| VS Code | 代码编辑器（必装插件：Tailwind CSS IntelliSense, Prettier, ESLint） |
| Windows Terminal | 终端 |
| Figma | 原型设计（可选，建议 MVP 后迭代） |
| Postman / Insomnia | API 调试 |

### 7.2 必备账号

| 账号 | 用途 |
|------|------|
| **GitHub** | 代码托管 |
| **DeepSeek / 智谱** | 大模型 API Key（AI Agent 用） |
| **阿里云** | OSS + CDN + 函数计算 或 ECS |

---

## 八、避坑指南与原则

### 8.1 MVP 原则（克制）

- ❌ 不一开始就引入向量数据库 — 结构化 JSON + Function Calling 足够
- ❌ 不引入 MDX — 纯 Markdown + gray-matter 更轻量
- ❌ 不一开始就做多 Agent 协同 — 单个 Agent + 3 个工具函数足矣
- ✅ 先让 AI 能回答基础问题，再逐步增强

### 8.2 国内网络适配

- 大模型优先使用国产：DeepSeek-V3 / 智谱 GLM-4（无需海外网络）
- 前端优先部署阿里云 OSS + CDN（Vercel 国内访问不稳定）

### 8.3 五行开发顺序建议

```
木（规划）→ 火（前端 UI）→ 土（数据内容）→ 金（AI 逻辑）→ 水（部署上线）
```

---

## 九、里程碑与时间线

| 里程碑 | 内容 | 预计工时 |
|--------|------|----------|
| M1 项目骨架就绪 | 初始化项目、配色配置、目录结构 | Day 1 |
| M2 数据层完成 | 知识卡片全部录入、读取工具就绪 | Day 2 |
| M3 主页完成 | 英雄区、技术栈、项目网格、导航页脚 | Day 3-4 |
| M4 详情页完成 | 项目详情页 SSG 渲染 | Day 5 |
| M5 AI 助手可用 | 对话界面 + API 联调 + 工具调用 | Day 6-7 |
| M6 部署上线 | 静态导出、OSS 部署、Agent 部署、CDN 加速 | Day 8 |
| M7 验收测试 | 全功能测试、移动端适配、性能检查 | Day 9 |

---

*文档生成日期：2026-07-15*
*依据：《个人简历网站提示词.md》 + 《个人简历网站-千问版本.md》 合并编制*
